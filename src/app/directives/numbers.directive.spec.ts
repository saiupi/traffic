import { NumbersDirective } from './numbers.directive';

describe('NumbersDirective', () => {
  it('should create an instance', () => {
    const directive = this.NumbersDirective();
    expect(directive).toBeTruthy();
  });
});
